[project]
name = "compress-pdf"
version = "0.1.0"
description = "Compress large pdf files into small sized files without losing quality"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
